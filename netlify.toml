[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18.20.0"
  NPM_VERSION = "10.5.0"

# Disable plugins that require higher Node versions
[[plugins]]
  package = "@netlify/plugin-lighthouse"
